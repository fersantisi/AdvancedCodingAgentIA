# --- Required ---------------------------------------------------------
OPENAI_API_KEY=sk-your-key-here

# --- Optional ----------------------------------------------------------
# Web search (https://tavily.com). Without it, web_search reports
# "not configured" but everything else works.
TAVILY_API_KEY=tvly-your-key-here

# LLM provider and model
LLM_PROVIDER=openai
OPENAI_MODEL=gpt-4o-mini

# Limits
COMMAND_TIMEOUT_SECONDS=60
MAX_TOOL_ITERATIONS=30
LLM_MAX_RETRIES=3

# Logging
AGENT_LOG_FILE=agent.log

# RAG (index built with: python -m coding_agent.rag.ingest <docs_dir>)
EMBEDDINGS_MODEL=text-embedding-3-small
RAG_STORE_PATH=rag_index.json

# Persistent project memory
AGENT_MEMORY_FILE=.agent/memory.json

# Observability backend: "none" or "langfuse" (requires the keys below).
OBSERVABILITY_PROVIDER=none

# Langfuse (https://langfuse.com). Only used when OBSERVABILITY_PROVIDER=langfuse.
# Use the host for your region: EU=https://cloud.langfuse.com, US=https://us.cloud.langfuse.com.
# LANGFUSE_BASE_URL is accepted as an alias for LANGFUSE_HOST.
LANGFUSE_PUBLIC_KEY=pk-lf-your-key-here
LANGFUSE_SECRET_KEY=sk-lf-your-key-here
LANGFUSE_HOST=https://cloud.langfuse.com

# Context compaction: summarize history once it exceeds this many messages
COMPACT_AFTER_MESSAGES=60
